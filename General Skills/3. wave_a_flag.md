Copy the binary in Kali Linux (because the file is a linux executable) then open the file directory in terminal.

Now use this following command:

```
chmod +x filename
./filename
```

```
Example:
chmod +x warm
./warm
./warm -h
```